import React from 'react';
import Header from '../components/Header';

const Layout = () => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
}

export default Layout;